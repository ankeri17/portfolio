[build]
  command = "npm run build"
  publish = "dist"

# Fonts never change without a filename change, so cache them hard.
[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
